# MSMP 集群部署环境变量（复制为 .env 后按需修改）

# PostgreSQL
POSTGRES_PASSWORD=msmp123

# 集群共享密钥（所有 server 节点必须一致）
MSMP_JWT_SECRET=change-me-in-production
# 凭据加密密钥（base64 编码的 32 字节 AES 密钥）——生产环境务必更换
MSMP_CREDENTIAL_KEY=vROhbPC9E2ONzrmN/HgktxJaAw2MVtBq7rw4jEORxKw=